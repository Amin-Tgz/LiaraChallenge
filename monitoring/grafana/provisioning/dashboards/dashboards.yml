apiVersion: 1

providers:
  - name: Liara Rescue
    orgId: 1
    folder: Liara Rescue
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
