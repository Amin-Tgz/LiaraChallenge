@AGENTS.md

# Claude Code

Read `RULES.md` before implementation.

Use OpenSpec artifacts under `openspec/changes/**` as the source of truth for any active task — the acceptance criteria live there, not in this file.

Specification documents, in precedence order:

1. `openspec/changes/<active>/` — the task at hand
2. `docs/deployment.md` — infrastructure, models, config, error taxonomy
3. `docs/liara-docs-rescue-plan.md` — product scope and Definition of Done
