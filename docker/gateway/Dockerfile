# Our own container of the open-source Portkey gateway, pinned — not Portkey's
# managed SaaS. Kept as an image of its own so the version is reviewable in the
# diff, the same way the monitoring services pin theirs.
FROM portkeyai/gateway:1.15.2
